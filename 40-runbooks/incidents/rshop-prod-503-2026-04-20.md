# rshop PROD — 503 (2026-04-20)

#incident #aws

## 1. Objaw / symptom

```
Data:       2026-04-20 ~09:00+
Środowisko: PROD (rshop-prod-Klaster, eu-central-1)
Objaw:      HTTP 503 Service Temporarily Unavailable
Źródło:     ALB bezpośrednio (Server: awselb/2.0) — CloudFront nie w łańcuchu
Status:     RCA potwierdzony
```

## 2. Zakres / scope

```
Dotknięte serwisy:
  - rshop-prod-backoffice-svc  → prod-backoffice-ALB-TG1 (EMPTY)
  - rshop-prod-frontend-svc1   → prod-frontend-ALB-TG (EMPTY)   [Renault domains]
  - rshop-prod-frontend-svc2   → prod-frontend2-ALB-TG (EMPTY)  [Dacia domains]

NIE dotknięte:
  - rshop-prod-api-svc         → prod-api-ALB-TG1 (healthy, 10.0.2.35)
  - akcesoria2 dacia/renault   → healthy

Domeny niedostępne:
  sklep.renault.pl, eshop.renault.cz, eshop.renault.sk, webshop.renault.hu
  sklep.dacia.pl, eshop.dacia.cz, eshop.dacia.sk, webshop.dacia.hu
  /admin, / (default rule → backoffice)
```

## 3. Root Cause

```
TagPolicyViolation — ECS nie może otagować ENI (Elastic Network Interface)
przy próbie uruchomienia taska Fargate.

Dowód (describe-tasks):
  StopCode:   TaskFailedToStart
  StopReason: Unexpected EC2 error while attempting to tag the network
              interface: TagPolicyViolation

Wszystkie trzy serwisy: desired=1, running=0, pending=0
ECS próbuje co ~13 min, za każdym razem TagPolicyViolation.

Hipoteza: nowa lub zmieniona Tag Policy w AWS Organizations wymaga
tagów których ECS nie dostarcza przy tworzeniu ENI.
```

## 4. Obrazy (z zatrzymanych tasków)

```
backoffice:  rshopapp-prod:backoffice.391
frontend-r:  rshopapp-prod:frontendr.599
frontend-d:  rshopapp-prod:frontendd.599
```

## 5. Szybkie komendy (read-only)

```bash
# Status serwisów
aws ecs describe-services \
  --cluster rshop-prod-Klaster \
  --services rshop-prod-backoffice-svc rshop-prod-frontend-svc1 rshop-prod-frontend-svc2 \
  --region eu-central-1 --profile rshop \
  --query 'services[*].{Name:serviceName,Desired:desiredCount,Running:runningCount}' \
  --output table

# Ostatnie zatrzymane taski
aws ecs list-tasks \
  --cluster rshop-prod-Klaster \
  --desired-status STOPPED \
  --region eu-central-1 --profile rshop

# Tag policies w Organizations
aws organizations list-policies \
  --filter TAG_POLICY \
  --profile rshop
```

## 6. Decision points

```
[ ] Sprawdzić kiedy zmieniono Tag Policy (Organizations → Tag policies → historia)
[ ] Ustalić jakich tagów brakuje — sprawdzić task definition vs wymagania policy
[ ] Fix: dodać wymagane tagi do task definition lub złagodzić/naprawić policy
[ ] Po naprawie: ECS automatycznie ponowi próbę uruchomienia (lub force deploy)
```

## 7. Findings / notes

```
2026-04-20 09:xx — pierwsze błędy w logach ECS (TagPolicyViolation)
2026-04-20 ~10:00 — incydent otwarty, RCA potwierdzony
```

---

*Ostatnia aktualizacja: 2026-04-20*
